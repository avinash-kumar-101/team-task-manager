{
  "name": "team-task-manager-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build && cd ../backend && npx prisma generate",
    "start": "cd backend && npx prisma migrate deploy && npm run start"
  }
}
